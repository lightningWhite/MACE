"""The declarative authoring flow, shared by the CLI and the web client.

Steps, field types, and query-backed selection — so every reference an author
makes is picked from things that exist, never typed.
See docs/09-authoring-and-wizard.md.
"""
